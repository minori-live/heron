name: Setup Heron build
description: Install the locked toolchain, native dependencies, caches, and workspace packages.

inputs:
  github-token:
    description: Token used by mise when downloading tools from GitHub.
    required: false
    default: ""
  cache-rust-build:
    description: >-
      Cache the Cargo target directory (and registry) between runs. Enable for
      check/test jobs that rebuild the same profiles every run; leave disabled
      for release packaging jobs to conserve the repository cache quota.
    required: false
    default: "false"

runs:
  using: composite
  steps:
    - name: Install Linux system dependencies
      if: runner.os == 'Linux'
      shell: bash
      run: |
        sudo apt-get update
        sudo apt-get install --yes --no-install-recommends \
          libasound2-dev \
          libcairo2-dev \
          libfontconfig1-dev \
          libfreetype-dev \
          libglib2.0-dev \
          libpango1.0-dev \
          libx11-dev \
          libx11-xcb-dev \
          libxcb1-dev \
          libxcb-cursor-dev \
          libxcb-keysyms1-dev \
          libxcb-util-dev \
          libxcb-xkb-dev \
          libxcursor-dev \
          libxi-dev \
          libxinerama-dev \
          libxkbcommon-dev \
          libxkbcommon-x11-dev \
          libopenjp2-tools \
          libxrandr-dev \
          libwayland-dev \
          wayland-protocols

    - name: Install locked toolchain
      uses: jdx/mise-action@v4
      with:
        version: 2026.7.13
        cache: true
        github_token: ${{ inputs.github-token }}

    - name: Expose libclang on Windows
      if: runner.os == 'Windows'
      shell: pwsh
      run: |
        $llvmRoot = mise where 'http:llvm'
        "LIBCLANG_PATH=$llvmRoot\bin" | Out-File -FilePath $env:GITHUB_ENV -Append

    - name: Prepare ASIO SDK on Windows
      if: runner.os == 'Windows'
      shell: pwsh
      run: |
        $downloadRoot = Join-Path $env:RUNNER_TEMP 'asio-sdk-download'
        $archivePath = Join-Path $env:RUNNER_TEMP 'asio-sdk.zip'
        New-Item -ItemType Directory -Path $downloadRoot -Force | Out-Null

        Invoke-WebRequest -Uri 'https://www.steinberg.net/asiosdk' -OutFile $archivePath
        Expand-Archive -Path $archivePath -DestinationPath $downloadRoot -Force

        $asioHeader = Get-ChildItem -Path $downloadRoot -Filter 'asio.h' -File -Recurse |
          Where-Object { $_.Directory.Name -eq 'common' } |
          Select-Object -First 1
        if ($null -eq $asioHeader) {
          throw 'The downloaded ASIO SDK does not contain common/asio.h'
        }

        $asioRoot = $asioHeader.Directory.Parent.FullName
        "CPAL_ASIO_DIR=$asioRoot" | Out-File -FilePath $env:GITHUB_ENV -Append

    - name: Restore Cargo target directory
      if: inputs.cache-rust-build == 'true'
      uses: Swatinem/rust-cache@v2
      with:
        # Warm caches come from main so pull requests restore a consistent
        # baseline without competing for the repository cache quota.
        save-if: ${{ github.ref == 'refs/heads/main' }}

    - name: Configure shared sccache backend
      shell: bash
      env:
        # PR caches are scoped to the merge ref and cannot warm main or other
        # PRs. Keep reads shared while concentrating writes on main/releases
        # to reduce the repository-wide GHA cache upload rate.
        CACHE_RW_MODE: ${{ (github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/')) && 'READ_WRITE' || 'READ_ONLY' }}
        CACHE_ERROR_LOG: ${{ runner.temp }}/heron-sccache-errors.log
      run: |
        echo "SCCACHE_GHA_ENABLED=true" >> "$GITHUB_ENV"
        printf 'SCCACHE_GHA_RW_MODE=%s\n' "$CACHE_RW_MODE" >> "$GITHUB_ENV"
        printf 'SCCACHE_ERROR_LOG=%s\n' "$CACHE_ERROR_LOG" >> "$GITHUB_ENV"
        echo "SCCACHE_LOG=warn" >> "$GITHUB_ENV"

    - name: Install sccache
      uses: mozilla-actions/sccache-action@v0.0.10
      with:
        # The sccache version participates in the GHA cache version. Avoid an
        # unplanned cold cache across every job when upstream publishes a release.
        version: v0.17.0

    - name: Enable sccache for subsequent build steps
      shell: bash
      run: |
        printf 'RUSTC_WRAPPER=%s\n' "${SCCACHE_PATH:-sccache}" >> "$GITHUB_ENV"

    - name: Locate Cargo and pnpm caches
      id: cache-paths
      shell: bash
      run: |
        echo "cargo-home=${CARGO_HOME:-$HOME/.cargo}" >> "$GITHUB_OUTPUT"
        echo "pnpm-store=$(pnpm store path --silent)" >> "$GITHUB_OUTPUT"

    - name: Restore Cargo downloads
      # rust-cache already caches the registry and git databases.
      if: inputs.cache-rust-build != 'true'
      uses: actions/cache@v5
      with:
        path: |
          ${{ steps.cache-paths.outputs.cargo-home }}/registry/index
          ${{ steps.cache-paths.outputs.cargo-home }}/registry/cache
          ${{ steps.cache-paths.outputs.cargo-home }}/git/db
        key: cargo-downloads-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('Cargo.lock') }}
        restore-keys: |
          cargo-downloads-${{ runner.os }}-${{ runner.arch }}-

    - name: Restore pnpm store
      uses: actions/cache@v5
      with:
        path: ${{ steps.cache-paths.outputs.pnpm-store }}
        key: pnpm-store-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('pnpm-lock.yaml') }}
        restore-keys: |
          pnpm-store-${{ runner.os }}-${{ runner.arch }}-

    - name: Restore Electron downloads
      uses: actions/cache@v5
      with:
        path: |
          ~/.cache/electron
          ~/.cache/electron-builder
          ~/Library/Caches/electron
          ~/Library/Caches/electron-builder
          ~/AppData/Local/electron/Cache
          ~/AppData/Local/electron-builder/Cache
        key: electron-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('pnpm-lock.yaml') }}
        restore-keys: |
          electron-${{ runner.os }}-${{ runner.arch }}-

    - name: Fetch pinned VST3 SDK
      shell: bash
      env:
        VST3_SDK_COMMIT: 9fad9770f2ae8542ab1a548a68c1ad1ac690abe0
      run: |
        if [[ ! -e third_party/vst3sdk/.git ]]; then
          git init third_party/vst3sdk
          git -C third_party/vst3sdk remote add \
            origin https://github.com/steinbergmedia/vst3sdk.git
          git -C third_party/vst3sdk fetch --depth 1 origin "$VST3_SDK_COMMIT"
          git -C third_party/vst3sdk checkout --detach FETCH_HEAD
          git -C third_party/vst3sdk submodule update --init --recursive --depth 1
        fi

        actual_commit="$(git -C third_party/vst3sdk rev-parse HEAD)"
        if [[ "$actual_commit" != "$VST3_SDK_COMMIT" ]]; then
          echo "::error::Expected VST3 SDK commit $VST3_SDK_COMMIT, got $actual_commit"
          exit 1
        fi

    - name: Fetch pinned ARA SDK
      shell: bash
      env:
        ARA_SDK_COMMIT: 0005c532bb03eb210e7bb490329fc6d7ba1792c7
      run: |
        if [[ ! -e third_party/ARA_SDK/.git ]]; then
          git init third_party/ARA_SDK
          git -C third_party/ARA_SDK remote add \
            origin https://github.com/Celemony/ARA_SDK.git
          git -C third_party/ARA_SDK fetch --depth 1 origin "$ARA_SDK_COMMIT"
          git -C third_party/ARA_SDK checkout --detach FETCH_HEAD
        fi

        actual_commit="$(git -C third_party/ARA_SDK rev-parse HEAD)"
        if [[ "$actual_commit" != "$ARA_SDK_COMMIT" ]]; then
          echo "::error::Expected ARA SDK commit $ARA_SDK_COMMIT, got $actual_commit"
          exit 1
        fi

        git -C third_party/ARA_SDK submodule update \
          --init \
          --depth 1 \
          ARA_API

        if [[ ! -f third_party/ARA_SDK/ARA_API/ARAInterface.h ]]; then
          echo "::error::ARA API submodule is missing ARAInterface.h"
          exit 1
        fi

    - name: Install workspace dependencies
      shell: bash
      run: mise run install
